import 'package:flutter/material.dart';

class ProviderScannerScreen extends StatelessWidget {
  const ProviderScannerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ProviderScannerScreen')),
      body: const Center(child: Text('ProviderScannerScreen Placeholder')),
    );
  }
}

