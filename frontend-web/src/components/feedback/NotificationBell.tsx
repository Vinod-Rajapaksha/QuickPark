import React from "react";

// Placeholder written so the provider header can load; no notification endpoint exists yet.
const NotificationBell: React.FC = () => null;

export default NotificationBell;
